package HW3;

public class Directory {
    public static AudioFile getFileValue() {
        AudioFile a = new AudioFile();
        if(a = null) throw new IllegalArgumentException ("Audio File must be not empty");
        return a;
    }
}
